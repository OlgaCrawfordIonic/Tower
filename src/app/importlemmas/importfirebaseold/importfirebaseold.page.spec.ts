import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ImportfirebaseoldPage } from './importfirebaseold.page';

describe('ImportfirebaseoldPage', () => {
  let component: ImportfirebaseoldPage;
  let fixture: ComponentFixture<ImportfirebaseoldPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ImportfirebaseoldPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
